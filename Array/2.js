let array = [2, 3, 4, 5];

// Adding a new element (1) to the beginning of the array
array.unshift(1);
// array.unshift(1, 2);

console.log(array); // Output: [1, 2, 3, 4, 5]

// unshift() modifies the original array and returns the new length of the array after adding the elements.
