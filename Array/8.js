// replace an item from array
let arr = [1, 2, 3, 4, 5];

// Replace element at index 2 with a new value
arr[2] = 6;

console.log(arr); // Output: [1, 2, 6, 4, 5]

//This method directly modifies the original array.
// if that index number does not exist then it gives undefined.